# Web features

Frontend code should be organized by user-facing capability rather than by backend domain internals. Early features are expected to include `terminal`, `world`, `history`, `directives`, `government`, `economy`, `knowledge` and later `map`.

The web client displays and submits authoritative simulation data; it must not reproduce simulation rules in TypeScript.
